module HPM.View
  (
    view
  ) where

-- Base Modules

-- Own Modules

view :: [String] -> IO ()
view (root) = do
  -- TODO : Check what Libaries are installed in root
  --        and later perhapse print some more information
